{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Hypermode Runtime (local)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "preLaunchTask": "go: generate code",
      "program": "${workspaceFolder}",
      "env": {
        "HYPERMODE_DEBUG": "true",
        "HYPERMODE_CLUSTER_DB": "postgresql://postgres:postgres@localhost:5433/my-runtime-db?sslmode=disable"
      },
      "args": [
        "--refresh=1s",
      ]
    },
    {
      "name": "Hypermode Runtime (AWS)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "preLaunchTask": "go: generate code",
      "program": "${workspaceFolder}",
      "env": {
        "HYPERMODE_DEBUG": "true",
        "AWS_REGION": "us-west-2",
        "AWS_PROFILE": "sandbox",
        "AWS_SDK_LOAD_CONFIG": "true",
        "HYPERMODE_CLUSTER_DB": "postgresql://postgres:postgres@localhost:5433/my-runtime-db?sslmode=disable"
      },
      "args": [
        "--useAwsSecrets",
        "--useAwsStorage",
        "--s3bucket=${input:s3bucket}",
        "--s3path=${input:s3folder}",
        "--refresh=1s",
      ]
    },
  ],
  "inputs": [
    {
      "id": "s3bucket",
      "type": "promptString",
      "description": "Enter the S3 bucket name",
      "default": "sandbox-runtime-storage"
    },
    {
      "id": "s3folder",
      "type": "promptString",
      "description": "Enter the S3 storage folder name",
      "default": "shared"
    },
  ]
}
